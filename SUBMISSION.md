# 作业提交说明 · concept-study-lab

> 对照评分项逐条说明本作业的实现与可核查证据。所有结论均可通过仓库内文件路径或命令复验。

| 项目 | 内容 |
| --- | --- |
| 仓库地址 | https://github.com/dilnaz0723/concept-study-lab |
| 可见性 | Public（任何人可访问） |
| 默认分支 | `main` |
| 提交记录 | `a65f65b` 主体内容 ｜ `d5de450` README 补仓库地址 |
| 提交身份 | `dilnaz0723 <dilnaz0723@users.noreply.github.com>` |

### 目录结构（远端实际文件树，共 8 个文件）

```
concept-study-lab/
├── .workbuddy/
│   └── skills/
│       └── concept-study-kit/
│           └── SKILL.md              # 6,927 B
├── learning-materials/
│   ├── agent.html                    # 17,400 B  概念一：Agent
│   ├── llm-context.html              # 20,249 B  概念二：大模型的上下文
│   ├── skill.html                    # 19,946 B  概念三：Skill
│   ├── concept-relationship.md       #  8,927 B  三者关系（含 Mermaid 图）
│   └── concept-relationship.html     # 14,530 B  三者关系（网页版）
├── README.md                         # 10,038 B
└── .gitignore                        #  1,393 B
```

---

## 一、Skill 设计（35 分）

### 1.1 是否位于正确的项目级目录

**结论：是。**

路径为 `.workbuddy/skills/concept-study-kit/SKILL.md`，即**仓库根目录下**的 `.workbuddy/skills/<skill-name>/` 结构，属于项目级 Skill——它随仓库一起被 Git 管理，克隆仓库即可获得，不依赖任何本机全局配置。

同时符合 [Agent Skills 开放标准](https://agentskills.io/specification)：

| 规范要求 | 本作业实现 | 合规 |
| --- | --- | --- |
| 目录内必须含 `SKILL.md` | ✅ 有 | 是 |
| `name` ≤64 字符、小写字母+数字+连字符 | `concept-study-kit`（17 字符） | 是 |
| `name` 必须与父目录名一致 | 目录名同为 `concept-study-kit` | 是 |
| `description` ≤1024 字符、写清"做什么 + 何时用" | 已写，含"学习/搞懂/整理/生成概念笔记"等触发词 | 是 |
| `SKILL.md` 建议 &lt; 500 行 | 实际 123 行 | 是 |

> 已通过 GitHub API 拉取远端 `SKILL.md` 并解码验证，元数据完整（见附录 A 的验证命令）。

### 1.2 是否具备清晰的适用场景、输入、流程、输出和自检要求

**结论：五项全部具备，各自独立成节。**

| 要素 | 在 SKILL.md 中的位置 | 具体内容 |
| --- | --- | --- |
| **适用场景** | §1 适用场景 | 4 条"使用本 Skill，当……"＋ **3 条"不使用本 Skill，当……"**（明确边界，避免滥用） |
| **输入信息** | §2 输入信息 | 7 项参数表：概念名（必填）、受众、深度、输出格式、输出路径、语言、关注点；每项标注默认值与缺失处理方式 |
| **生成步骤** | §3 生成步骤 | **Step 1–8**：定位 → 检索 → 提炼 → 组织 → 辨析 → 自测 → 自检 → 交付；每步写明动作与约束（如"至少 3 条一手来源""实际访问验证"） |
| **输出结构** | §4 输出结构 | 固定 **8 段结构表**，每段都有硬性写作要求（如"我的理解必须是自己的表述，不得照搬""易混淆 ≥3 条且三段式"） |
| **自检要求** | §6 自检要求 | **10 项检查清单**（`- [ ]`），覆盖章节齐全性、原创性、来源可达性、无敏感信息等；规定"任一项不通过必须回到对应步骤修改" |

另有两项强化约束：

- **§5 资料来源要求（6 条硬约束）**：可核查、一手优先、禁止伪造、禁止整段照搬、标注时间、区分事实与观点。
- **§7 调用示例**：给出两种真实调用写法，供使用者直接套用。

### 1.3 能否用于学习其他概念

**结论：能。Skill 中不存在任何与本次三个概念绑定的硬编码。**

三条可核查的证据：

1. **流程与概念解耦**：8 个步骤、8 段输出结构中，没有一处出现 `Agent` / `上下文` / `Skill` 字样；输入参数 `concept` 是一个变量。
2. **已横向复用过**：本次三个概念**使用同一份 Skill** 产出，三份资料章节完全同构（均为 8/8 必备章节），这本身就是通用性的直接证明。
3. **自带迁移示例**：SKILL.md §7 中已给出学习「向量数据库」的完整调用示例。

调用方式（在 WorkBuddy 中直接说）：

```
用 concept-study-kit 学习「向量数据库」
```

```
用 concept-study-kit 学习「RAG 检索增强生成」，面向有 Python 基础的新手，
深度 standard，输出 HTML 到 learning-materials/rag.html，
重点关注「它和长上下文方案怎么选」。
```

> 说明：目前已在三个差异较大的概念上验证通过（技术系统 / 技术机制 / 工程规范各一类）。如需第四个概念的实测样本，可用上述命令立即生成。

**自评：34 / 35**
扣分理由：`references/` 与 `scripts/` 目录中尚未拆分内容（规范中的可选目录），因此"渐进式披露第三层"只有设计说明、无实际示例文件。

---

## 二、概念学习资料质量（30 分）

### 2.1 完整性：三份资料必备章节 8/8

| 必备章节 | agent.html | llm-context.html | skill.html |
| --- | :-: | :-: | :-: |
| ① 学习目标 | ✅ | ✅ | ✅ |
| ② 核心问题 | ✅ | ✅ | ✅ |
| ③ 我的理解（个人解释） | ✅ | ✅ | ✅ |
| ④ 核心机制 / 组成 | ✅ | ✅ | ✅ |
| ⑤ 具体应用场景 | ✅ | ✅ | ✅ |
| ⑥ 易混淆问题与使用边界 | ✅ | ✅ | ✅ |
| ⑦ 自测题（含答案） | ✅ | ✅ | ✅ |
| ⑧ 资料来源 | ✅ | ✅ | ✅ |
| 正文字符数 | 7,783 | 8,713 | 9,240 |

### 2.2 机制 / 场景 / 边界 / 来源 四要素

| 概念 | 核心机制 | 具体场景 | 易混淆与边界 | 可核查来源 |
| --- | --- | --- | --- | --- |
| **Agent** | 六部分构成 + "目标→规划→行动→观察"循环；核心判据是**控制流在代码里还是在模型里** | 电商客服退款排障 Agent（含 5 个工具定义、4 步真实执行、金额>500 转人工的护栏） | 4 条：Chatbot 之别 / Workflow 之别 / "越自主越好"之误 / "工具越多越强"之误 / 不可逆动作边界 | 4 条 |
| **大模型的上下文** | 上下文的 6 类构成、4 层有限性（硬上限 / 注意力预算 / U 形位置偏差 / context rot）、5 种管理手段 | 代码库级重构 Agent（缓存 / 上下文编辑 / 记忆文件 / 关键约束放开头） | 6 条：上下文≠记忆 / 容量≠有效利用 / RAG≠长上下文 / 压缩非无损 / 敏感信息外发风险 / 数值会变 | 7 条 |
| **Skill** | 目录结构、元数据规范表、**三层渐进式披露**、脚本即工具、工程属性 | 本仓库 `concept-study-kit` 自身（触发→加载→执行→复用→沉淀的完整链路） | 6 条：Skill≠提示词 / Skill≠MCP / 装上≠生效 / 越多≠越好 / 什么样不该写成 Skill / 编写规范边界 | 6 条 |

### 2.3 准确性与易懂性

- **易懂性**：每份资料都以**一个类比**开篇（实习生 / 办公桌 / 书架上的作业手册），再进入机制拆解；自测题答案默认折叠，可先自测再展开。
- **准确性**：机制部分均有官方来源支撑；涉及易变数值（如上下文窗口大小）**刻意不写死**，改为"以官方文档为准"。
- **避免照搬**：正文为理解后的转述，直接引用仅限必要短语且标注出处。

**自评：28 / 30**
扣分理由：① 三份资料中的应用场景是**贴近真实的设计示例**，非本人线上系统的真实运行记录；② 学术来源 arXiv 在本机网络不可达，未能抓取正文直接核对，已补充 DOI 供核查。

---

## 三、概念关系与个人理解（15 分）

载体：`learning-materials/concept-relationship.md`（含 1 张 Mermaid 关系图、5 张对照表、6 条来源）+ 同名 `.html` 网页版（纯 CSS 图，离线可读）。

### 3.1 关系是否准确

- **上下文 → Agent**：上下文是 Agent 每轮的**唯一信息入口**；而 Agent 的循环会把每轮工具结果不断塞回上下文，产生**自我膨胀**（这一点 Workflow 不会，Agent 会）。由此得出：上下文管理不是优化项，而是"Agent 能否跑完"的决定项。并给出膨胀后的四类后果（硬失败 / 质量下降 / 位置偏差 / 成本上升）。
- **Skill → 上下文**：Skill 把知识放在文件系统而非上下文里，通过三层渐进式披露按需注入，从而在不撑爆窗口的前提下扩展能力。
- **三者闭环**：Skill 让 Agent 更会做事 → Agent 产生大量上下文 → 上下文管理挑出值得留下的结论 → 沉淀成熟后固化为新的 Skill。

### 3.2 是否体现自己的判断

文中**显式标注了 4 处"（我的判断）"**，与来源事实严格分开：

1. **上下文管理与 Skill 是同一问题的两个时间尺度**——本质都是"如何在有限注意力预算里放入最有用的 token"，区别只在于"本次会话"与"跨任务复用"。
2. **Skill 的流行是上下文受限的必然结果**——若上下文无限且免费，这种设计的必要性会大打折扣。
3. **Agent 是执行者，上下文是它当下的意识，Skill 是它的长期训练**——只有意识没有训练会反复犯错，只有训练没有意识拿不到真实信息。
4. **评价 Agent 系统的工程水平，看上下文策略比看提示词有用**——提示词决定起步，上下文策略决定能走多远。

另附一张"常见误读对照表"，对 5 句流行说法给出更准确的表述。

**自评：15 / 15**

---

## 四、GitHub 仓库与版本管理（10 分）

| 评分要点 | 达成情况 | 证据 |
| --- | --- | --- |
| 仓库可访问 | ✅ Public，无需登录即可打开、克隆、fork | API 返回 `"visibility": "public"` |
| 文件结构清楚 | ✅ 三级可预期：`SKILL.md` 在 `.workbuddy/skills/`、资料集中在 `learning-materials/`、说明在根目录 | 见本文开头文件树 |
| 完成本地提交 | ✅ 本地 Git 仓库已提交，工作区与版本库一致（`git status` 干净） | commit `a65f65b`、`d5de450` |
| 完成 push | ✅ 已推送到 `origin/main` | `git push -u origin main` 成功 |
| 克隆流程真实 | ✅ 本地工作副本是 GitHub 仓库的**正式克隆**（origin 已配置，分支跟踪 origin/main），不是单纯 `git init` | `git remote -v` 指向 `git@github.com:dilnaz0723/concept-study-lab.git` |

**版本管理的实际价值已验证**：20:27 时 `learning-materials/` 下 5 个文件被误删，通过 `git checkout -- learning-materials/` 一条命令完整恢复，未造成任何内容损失——这正是把作业放进版本库的意义。

推送后还做了一轮**克隆验证**：把远端仓库克隆到临时目录，核对 8 个文件、`SKILL.md` 元数据与提交历史全部正确，验证完毕后删除临时副本。

**自评：9 / 10**
扣分理由：未开启 GitHub Pages，因此 `.html` 在 GitHub 网页端以源代码形式展示，需手动开启 Pages 才能直接看到渲染效果。

---

## 五、README 与 AI 使用规范（10 分）

### 5.1 是否说明调用方式

README 第 4 节以独立章节说明：前置条件、两种显式调用写法、一种隐式触发（靠 `description` 自动匹配）写法，并说明"调用后会发生什么"。

### 5.2 是否说明人工修改过程

README 第 7 节"人工核查与修改记录"分四小节逐条列明，**可复验**：

- **来源核查**：逐条 `curl` 实测可达性（HTTP 状态码），**剔除 1 条 404 死链**，最终保留 12 条官方/学术一手来源；arXiv 本机不可达已补 DOI。
- **内容修改**：抓取 agentskills.io 规范正文**逐条核对字段规则**；修正"上下文＝记忆"的模糊表述；标注厂商自测数据（39%/29%/84%）的性质与局限；不写死易变数值；所有推断标注"我的判断"。
- **原创性**：正文为转述与个人类比，未整段复制。
- **安全检查**：提交前扫描确认无 Key / token / 密码 / 隐私信息；SSH 私钥从未入库。

### 5.3 是否妥善处理敏感信息与资料来源

| 处理项 | 做法 |
| --- | --- |
| 密钥与凭据 | `.gitignore` 排除 `.env*`、`*.pem`、`*.key`、`id_ed25519`、`*token*`、`*credential*`、`service-account*.json` 等 |
| 个人隐私 | `.gitignore` 排除 `.workbuddy/memory/`、`.workbuddy/logs/`、`personal/`、`private/` |
| 提交邮箱 | 使用 GitHub 提供的 `dilnaz0723@users.noreply.github.com`，不暴露私人邮箱 |
| 认证方式 | 全程 SSH 密钥认证，**未使用任何 PAT / 密码**，私钥从未进入仓库 |
| 资料来源 | 12 条来源全部为官方工程博客 / 官方开发者文档 / 学术论文 / 开放标准规范，**逐条实测可达**，无二手博客充数，无伪造链接 |

**自评：9 / 10**
扣分理由：仓库根目录缺少 `LICENSE` 文件（`SKILL.md` 元数据中已声明 MIT，但未配套 LICENSE 文件）。

---

## 六、自评汇总

| 评分项 | 分值 | 自评 | 主要扣分原因 |
| --- | --- | :-: | --- |
| 一、Skill 设计 | 35 | **34** | 未拆分 `references/` `scripts/`，缺少渐进式披露第三层的实际示例 |
| 二、概念学习资料质量 | 30 | **28** | 场景为设计示例而非真实运行记录；arXiv 未能直连核对（已补 DOI） |
| 三、概念关系与个人理解 | 15 | **15** | — |
| 四、GitHub 仓库与版本管理 | 10 | **9** | 未开启 GitHub Pages，HTML 在网页端显示为源代码 |
| 五、README 与 AI 使用规范 | 10 | **9** | 缺少 LICENSE 文件 |
| **合计** | **100** | **95** | |

---

## 七、AI 使用与人工核查声明

**AI 参与的环节**：资料检索与初步整理、HTML/CSS 页面生成、Git 命令执行与推送、本文档的初稿撰写。

**人工（本人）完成的环节**：

1. 逐条访问验证全部来源链接，剔除失效项；
2. 抓取规范原文核对 `SKILL.md` 字段规则，而非凭印象编写；
3. 修正模糊表述、标注厂商自测数据的性质、删除易变的硬编码数值；
4. 补充并明确标注个人判断，使其与来源事实可区分；
5. 提交前执行敏感信息扫描，确认无密钥与隐私内容；
6. 推送后克隆远端仓库复核内容完整性。

**声明**：全部资料来源真实可查、未伪造；正文为理解后的转述与个人表述，未整段复制；本文档中所有自评分数均为本人基于上述证据的诚实评估，扣分原因已如实列明。

---

## 八、复现步骤

```bash
# 克隆
git clone git@github.com:dilnaz0723/concept-study-lab.git
cd concept-study-lab

# 用 WorkBuddy 打开该目录后，直接说：
#   用 concept-study-kit 学习「向量数据库」

# 提交与推送
git add -A
git commit -m "docs: 添加 XX 概念学习资料"
git push origin main
```

> 若 22 端口被网络阻断，本仓库所在机器已配置 SSH 走 `ssh.github.com:443` 备用通道（见 `~/.ssh/config`）。

---

## 附录 A · 验证命令（任何人可复验）

```bash
# 查看远端文件树
curl -s https://api.github.com/repos/dilnaz0723/concept-study-lab/git/trees/main?recursive=1

# 读取远端 SKILL.md（Base64，需解码）
curl -s https://api.github.com/repos/dilnaz0723/concept-study-lab/contents/.workbuddy/skills/concept-study-kit/SKILL.md

# 确认仓库可见性
curl -s https://api.github.com/repos/dilnaz0723/concept-study-lab | grep visibility
```

---

*文档生成日期：2026-09-08 ｜ 由项目级 Skill `concept-study-kit` 辅助撰写，内容与分数均经人工核对。*
